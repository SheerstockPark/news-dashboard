name: Urgent-alert audit (one-shot)

# Dumps the urgent-alert send history + article pool from the cloud DB to
# docs/urgent-audit.json and commits it, so thresholds can be retuned locally on real data.
# Fires on pushes that touch the audit script (self-triggering: push the script, get the data).
on:
  push:
    paths:
      - "tools/audit_urgent.py"
      - ".github/workflows/urgent-audit.yml"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: urgent-audit
  cancel-in-progress: false

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install dependencies
        run: pip install -r requirements-brief.txt
      - name: Dump send history + pool
        env:
          TURSO_DATABASE_URL: ${{ secrets.TURSO_DATABASE_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}
        # Capture the traceback to a committed file — the repo owner analyses results locally
        # and unauthenticated API access can't read Actions logs, so errors must travel by git.
        run: |
          touch docs/urgent-audit-error.txt
          python tools/audit_urgent.py --since 2026-07-05 --out docs/urgent-audit.json \
            2> docs/urgent-audit-error.txt || true
          if [ -s docs/urgent-audit-error.txt ]; then cat docs/urgent-audit-error.txt; fi
      - name: Commit results
        run: |
          git config user.name "sheerstock-audit"
          git config user.email "actions@users.noreply.github.com"
          git add -f docs/urgent-audit-error.txt || true
          git add -f docs/urgent-audit.json || true
          git commit -m "Urgent-alert audit data (auto)" || { echo "no changes"; exit 0; }
          git pull --rebase origin main
          git push origin main
