# Deps for the scheduled briefing + urgent-alert crons. Heavier than the ingest cron
# (adds the Anthropic SDK for the AI brief and yfinance for the live price tape) but still
# no Streamlit/pandas/charts. See .github/workflows/briefing.yml and alerts.yml.
anthropic==0.112.0
feedparser==6.0.12
PyYAML==6.0.3
python-dotenv==1.2.1
requests==2.32.5
yfinance==1.2.0
tzdata==2025.2
libsql; platform_system == "Linux"
